# Auto-detect text files and normalize line endings
* text=auto

# Windows scripts
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Shell scripts
*.sh text eol=lf

# Source/text files (normalize to LF)
*.py  text eol=lf
*.js  text eol=lf
*.css text eol=lf
*.html text eol=lf
*.json text eol=lf
*.md  text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.txt text eol=lf

# =====================
# Git LFS - Large Files
# =====================

# CSV files (tracked by LFS)
*.csv filter=lfs diff=lfs merge=lfs -text

# RAR archives (tracked by LFS)
*.rar filter=lfs diff=lfs merge=lfs -text

# All files in docs/ directory (tracked by LFS)
docs/** filter=lfs diff=lfs merge=lfs -text

